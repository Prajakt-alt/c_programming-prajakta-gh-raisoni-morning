//withought argument withought returntype

#include<stdio.h>
#include<math.h>
void armstrongnumber(){
	int sum=0;
	int number153;
	int remainder,original=sum,temp,count=0;
	original-sum = number;
	temp=number;
	while(temp!=0)
	{
		remainder=temp%10;
		sum=sum+round(pow(reaminder,cout));
		temp=temp/10;
	}
	if(original=sum==sum)
	}
	printf("the number is armstrong number");
}
else{
	printf("the number is not armstrong number");
}
void main();
armstrong number();
}

